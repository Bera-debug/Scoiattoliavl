
  # Transfer project to subscription

  This is a code bundle for Transfer project to subscription. The original project is available at https://www.figma.com/design/Z1QdG1TWUq7rfm42batbmQ/Transfer-project-to-subscription.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  